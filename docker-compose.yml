services:
  postgres:
    image: postgres:16
    container_name: kpiapi-postgres
    environment:
      POSTGRES_DB: kpiapi
      POSTGRES_USER: kpiapi
      POSTGRES_PASSWORD: ${KPIAPI_DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - kpiapi_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kpiapi -d kpiapi"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrate
    container_name: kpiapi-migrate
    environment:
      ConnectionStrings__DefaultConnection: Host=postgres;Port=5432;Database=kpiapi;Username=kpiapi;Password=${KPIAPI_DB_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: final
    container_name: kpiapi-api
    environment:
      ASPNETCORE_URLS: http://+:8080
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__DefaultConnection: Host=postgres;Port=5432;Database=kpiapi;Username=kpiapi;Password=${KPIAPI_DB_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    ports:
      - "8080:8080"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: kpiapi-frontend
    depends_on:
      - api
    ports:
      - "3000:80"
    restart: unless-stopped

volumes:
  kpiapi_pgdata: